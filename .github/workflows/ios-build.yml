name: Build iOS

on:
  workflow_dispatch:

jobs:
  build:
    name: Build iOS IPA
    runs-on: macos-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Build iOS Application
        run: |
          flutter pub get
          # This high-level command is the most reliable way to build for iOS in CI.
          # It manages kernel builds, plugin registration, and framework embedding correctly.
          flutter build ios --release --no-codesign
      - name: Create IPA
        run: |
          # Manual packaging of the produced .app bundle into a .ipa file
          mkdir -p Payload
          # Locate the .app bundle
          APP_PATH=$(find build/ios/iphoneos -name "*.app" | head -n 1)
          echo "Found app at: $APP_PATH"
          cp -r "$APP_PATH" Payload/
          zip -r QiblaTime.ipa Payload
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: QiblaTime-IPA
          path: QiblaTime.ipa
